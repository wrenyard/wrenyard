export const WORKER_SPRITE_W = 40;
export const WORKER_SPRITE_H = 44;

// The mascot drawing is vertically centered inside the 44px sprite box and
// only paints through the foot/shadow line at about 32px. Window placement
// should align this visible footline, not the transparent sprite box.
export const WORKER_VISIBLE_H = 32;
export const WORKER_WINDOW_TOP_PADDING = 104;
